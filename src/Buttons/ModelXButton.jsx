

function ModelXButton({ text, showImg }) {
    return (
        <div>
            <button className="demo-drive-selctor-btn" onClick={showImg}>{text}</button>
        </div>
    )
}

export default ModelXButton
